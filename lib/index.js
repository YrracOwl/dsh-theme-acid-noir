// Acid Noir host anchor. The browser half owns the selectable theme catalog
// and Settings row; no Host authority or filesystem access is required.
export const name = 'dsh-theme-acid-noir'
export function apply() {}
